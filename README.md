git init
git config user.email "gruhbsalta@gmail.com"
git config user.name "Gustavo"
git add index.html manifest.json
git commit -m "Versión inicial"
git remote add origin https://github.com/gruhbsalta-web/gustavo-finanzas.git
git branch -M main
git push -u origin main
